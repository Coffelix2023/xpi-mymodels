## 当前状态总结

 设计决策阶段已完成。用户通过可视化卡片选择了四个关键架构方向：

 ### 已确定的架构选择

 1. **持久化策略：文件优先**
    - 校验后原子写入 `~/.pi/agent/models.json`
    - 当前会话需要用户重新打开 `/model` 刷新
    - 实现成本最低，重启行为最可预测

 2. **表单组织：分组折叠**
    - 基础字段（baseUrl、api、apiKey）默认展开
    - Models、Thinking、Compatibility、Sampling、Pricing 按组折叠
    - 符合 Pi TUI 的纵向滚动习惯

 3. **Ctrl+P 管理：独立循环列表**
    - 左侧显示所有可用模型
    - 右侧单独维护快速切换顺序
    - 语义最清楚：可用性 ≠ 循环资格

 4. **价格输入：USD/CNY 归一**
    - 界面支持两种币种输入
    - 保存时统一转换为 Pi 识别的 USD / 1M tokens
    - 与 Pi 内置费用计算直接兼容

 ### 待解决的实现细节

 1. **汇率来源与快照**：USD/CNY 转换需要明确汇率数据源（固定值 vs 实时 API vs 用户配置）
 2. **models.json 校验**：需要完整的 JSON Schema 或类型定义来拦截保存前的错误
 3. **连通性测试**：价格卡片中的"费率预览+测试"未被选择，但独立测试功能仍可作为高级特性提供
 4. **原子写入与备份**：需要实现 `.models.json.backup` 和写入失败时的回滚逻辑

 ### 建议的下一步

 **选项 A：直接实现核心流程**
 1. 搭建 TUI 表单框架（基于 `@earendil-works/pi-tui`）
 2. 实现 models.json 读取、校验、原子写入
 3. 完成分组折叠表单和独立循环列表 UI
 4. 集成价格字段和币种转换

 **选项 B：先完善技术文档**
 1. 将架构选择写入 `DESIGN.md`
 2. 补充 models.json 的完整类型定义和校验规则
 3. 明确扩展命令注册（如 `/my-models`）和快捷键
 4. 确定汇率策略后再开始编码

 **推荐路径：A + 关键决策确认**
 - 直接开始实现核心 TUI 和 models.json 操作
 - 汇率暂时使用**固定值 7 CNY/USD**，并在界面标注"固定汇率"
 - 连通性测试作为 v2 特性，先完成配置编辑的主路径
 - 边实现边补充 `DESIGN.md` 和类型定义

 **需要你确认的关键问题：**
 1. 汇率策略：固定值 OK，还是希望实时获取或用户配置？
 2. 扩展入口：通过 `/my-models` 命令还是快捷键（如 `Ctrl+M`）？
 3. 是否需要在首版包含模型连通性测试？

 选择你偏好的路径，或直接说"开始实现"我会按推荐路径进行。
